import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gestion-annonce1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
